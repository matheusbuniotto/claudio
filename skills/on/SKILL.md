Enable claudio-tts by removing the disabled flag file.

Run this command:

```bash
rm -f ~/.claude/claudio-tts.disabled
```

Then confirm to the user: "claudio-tts enabled — Claude will speak after every response."
