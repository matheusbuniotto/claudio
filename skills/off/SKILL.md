Disable claudio-tts by creating the disabled flag file.

Run this command:

```bash
touch ~/.claude/claudio-tts.disabled
```

Then confirm to the user: "claudio-tts disabled — run /claudio-tts:on to re-enable."
